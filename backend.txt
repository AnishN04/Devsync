# 🖥️ DevSync — Backend Architecture & Structure

> **Stack:** Node.js · Express · PostgreSQL · JWT Auth · RBAC · Socket.io

---

## 📁 Folder Structure

```
server/
├── config/
│   ├── db.js                        # PostgreSQL pool connection
│   └── env.js                       # Environment variable loader
│
├── controllers/
│   ├── auth.controller.js           # register, login, refresh, logout
│   ├── project.controller.js        # CRUD for projects
│   ├── task.controller.js           # CRUD for tasks + socket emit
│   ├── member.controller.js         # Add/remove project members
│   └── ai.controller.js             # AI task suggestion handler
│
├── routes/
│   ├── auth.routes.js               # /api/auth/*
│   ├── project.routes.js            # /api/projects/*
│   ├── task.routes.js               # /api/tasks/*
│   ├── member.routes.js             # /api/members/*
│   └── ai.routes.js                 # /api/ai/*
│
├── middleware/
│   ├── verifyToken.js               # JWT verification for REST routes
│   ├── checkRole.js                 # RBAC role guard middleware
│   ├── socketAuth.js                # JWT verification for socket connections
│   └── errorHandler.js             # Global error handler
│
├── services/
│   ├── auth.service.js              # Business logic: hashing, token gen
│   ├── project.service.js           # Business logic: project ops
│   ├── task.service.js              # Business logic: task ops
│   └── ai.service.js                # Gemini/OpenAI API calls
│
├── models/
│   ├── user.model.js                # DB queries for users table
│   ├── project.model.js             # DB queries for projects table
│   ├── task.model.js                # DB queries for tasks table
│   └── member.model.js              # DB queries for project_members table
│
├── sockets/
│   ├── index.js                     # Socket.io server init + middleware
│   ├── events.js                    # Central event name constants
│   └── handlers/
│       ├── task.handler.js          # Task status change events
│       ├── project.handler.js       # Project-level events
│       ├── notification.handler.js  # User notifications
│       └── presence.handler.js      # Online/offline team presence
│
├── db/
│   └── schema.sql                   # Full PostgreSQL schema
│
├── .env                             # Environment variables (never commit)
├── .env.example                     # Template for env vars
├── package.json
└── server.js                        # Entry point
```

---

## 🗃️ PostgreSQL Database Schema (`db/schema.sql`)

```sql
-- Users table
CREATE TABLE users (
  id            SERIAL PRIMARY KEY,
  name          VARCHAR(100) NOT NULL,
  email         VARCHAR(150) UNIQUE NOT NULL,
  password_hash TEXT NOT NULL,
  role          VARCHAR(20) CHECK (role IN ('Admin', 'Manager', 'Developer', 'Viewer')) DEFAULT 'Developer',
  created_at    TIMESTAMP DEFAULT NOW()
);

-- Projects table
CREATE TABLE projects (
  id          SERIAL PRIMARY KEY,
  title       VARCHAR(200) NOT NULL,
  description TEXT,
  owner_id    INTEGER REFERENCES users(id) ON DELETE SET NULL,
  created_at  TIMESTAMP DEFAULT NOW()
);

-- Project members (junction table with role per project)
CREATE TABLE project_members (
  id          SERIAL PRIMARY KEY,
  project_id  INTEGER REFERENCES projects(id) ON DELETE CASCADE,
  user_id     INTEGER REFERENCES users(id) ON DELETE CASCADE,
  role        VARCHAR(20) CHECK (role IN ('Manager', 'Developer', 'Viewer')) NOT NULL,
  joined_at   TIMESTAMP DEFAULT NOW(),
  UNIQUE(project_id, user_id)
);

-- Tasks table
CREATE TABLE tasks (
  id          SERIAL PRIMARY KEY,
  project_id  INTEGER REFERENCES projects(id) ON DELETE CASCADE,
  title       VARCHAR(300) NOT NULL,
  description TEXT,
  status      VARCHAR(30) CHECK (status IN ('Todo', 'In Progress', 'Done')) DEFAULT 'Todo',
  priority    VARCHAR(20) CHECK (priority IN ('Low', 'Medium', 'High')) DEFAULT 'Medium',
  assigned_to INTEGER REFERENCES users(id) ON DELETE SET NULL,
  due_date    DATE,
  created_at  TIMESTAMP DEFAULT NOW()
);

-- Refresh tokens table
CREATE TABLE refresh_tokens (
  id          SERIAL PRIMARY KEY,
  user_id     INTEGER REFERENCES users(id) ON DELETE CASCADE,
  token       TEXT UNIQUE NOT NULL,
  created_at  TIMESTAMP DEFAULT NOW(),
  expires_at  TIMESTAMP NOT NULL
);
```

---

## 🚀 Server Entry Point (`server.js`)

```js
const express       = require('express');
const http          = require('http');
const { Server }    = require('socket.io');
const cors          = require('cors');
require('dotenv').config();

const authRoutes    = require('./routes/auth.routes');
const projectRoutes = require('./routes/project.routes');
const taskRoutes    = require('./routes/task.routes');
const aiRoutes      = require('./routes/ai.routes');
const errorHandler  = require('./middleware/errorHandler');
const initSockets   = require('./sockets/index');

const app    = express();
const server = http.createServer(app);   // wrap Express in HTTP server
const io     = new Server(server, {
  cors: {
    origin:  process.env.CLIENT_URL,     // e.g. http://localhost:5173
    methods: ['GET', 'POST']
  }
});

app.use(cors());
app.use(express.json());

// Make io accessible inside REST controllers
app.set('io', io);

// REST API routes
app.use('/api/auth',     authRoutes);
app.use('/api/projects', projectRoutes);
app.use('/api/tasks',    taskRoutes);
app.use('/api/ai',       aiRoutes);
app.use(errorHandler);

// WebSocket init
initSockets(io);

server.listen(process.env.PORT, () =>
  console.log(`Server running on port ${process.env.PORT}`)
);
```

> Socket.io and REST APIs share the **same port** via `http.createServer(app)`.

---

## 🔐 Authentication Flow

```
POST /api/auth/register
  → Validate input
  → Hash password with bcrypt (salt rounds: 10)
  → Save user to DB
  → Return 201

POST /api/auth/login
  → Find user by email
  → Compare password with bcrypt
  → Generate Access Token  (JWT, 15min)
  → Generate Refresh Token (JWT, 7d)
  → Store refresh token in DB
  → Return both tokens

POST /api/auth/refresh
  → Verify refresh token exists in DB
  → Verify JWT signature
  → Issue new access token
  → Return new access token

POST /api/auth/logout
  → Delete refresh token from DB
  → Return 200
```

---

## 🛡️ REST Middleware

### `middleware/verifyToken.js`

```js
const jwt = require('jsonwebtoken');

const verifyToken = (req, res, next) => {
  const authHeader = req.headers['authorization'];
  const token = authHeader && authHeader.split(' ')[1]; // Bearer <token>

  if (!token) return res.status(401).json({ message: 'No token provided' });

  try {
    const decoded = jwt.verify(token, process.env.JWT_SECRET);
    req.user = decoded; // attach user to request
    next();
  } catch (err) {
    return res.status(403).json({ message: 'Invalid or expired token' });
  }
};

module.exports = verifyToken;
```

### `middleware/checkRole.js`

```js
const checkRole = (allowedRoles) => (req, res, next) => {
  if (!allowedRoles.includes(req.user.role)) {
    return res.status(403).json({ message: 'Access denied: insufficient role' });
  }
  next();
};

module.exports = checkRole;
```

### Usage in Routes

```js
router.delete('/projects/:id', verifyToken, checkRole(['Admin']),                      deleteProject);
router.post('/tasks',          verifyToken, checkRole(['Admin','Manager','Developer']), createTask);
router.get('/projects',        verifyToken,                                             getAllProjects);
```

---

## 👥 RBAC — Roles & Permissions

| Permission           | Admin | Manager | Developer | Viewer |
|----------------------|:-----:|:-------:|:---------:|:------:|
| Create Project       | ✅    | ✅      | ❌        | ❌     |
| Delete Project       | ✅    | ❌      | ❌        | ❌     |
| Manage Members       | ✅    | ✅      | ❌        | ❌     |
| Create Task          | ✅    | ✅      | ✅        | ❌     |
| Update Task Status   | ✅    | ✅      | ✅        | ❌     |
| Delete Task          | ✅    | ✅      | ❌        | ❌     |
| View Projects/Tasks  | ✅    | ✅      | ✅        | ✅     |

---

## 🌐 REST API Endpoints

### Auth — `/api/auth`

| Method | Endpoint    | Description           | Auth |
|--------|-------------|-----------------------|------|
| POST   | `/register` | Register new user     | ❌   |
| POST   | `/login`    | Login, get tokens     | ❌   |
| POST   | `/refresh`  | Get new access token  | ❌   |
| POST   | `/logout`   | Invalidate token      | ✅   |

### Projects — `/api/projects`

| Method | Endpoint | Description           | Roles          |
|--------|----------|-----------------------|----------------|
| GET    | `/`      | Get all user projects | All            |
| POST   | `/`      | Create project        | Admin, Manager |
| GET    | `/:id`   | Get project by ID     | All            |
| PUT    | `/:id`   | Update project        | Admin, Manager |
| DELETE | `/:id`   | Delete project        | Admin          |

### Tasks — `/api/tasks`

| Method | Endpoint | Description          | Roles                     |
|--------|----------|----------------------|---------------------------|
| GET    | `/`      | Get tasks by project | All                       |
| POST   | `/`      | Create task          | Admin, Manager, Developer |
| PUT    | `/:id`   | Update task          | Admin, Manager, Developer |
| DELETE | `/:id`   | Delete task          | Admin, Manager            |

### Members — `/api/members`

| Method | Endpoint               | Description         | Roles          |
|--------|------------------------|---------------------|----------------|
| GET    | `/:projectId`          | Get project members | All            |
| POST   | `/:projectId`          | Add member          | Admin, Manager |
| DELETE | `/:projectId/:userId`  | Remove member       | Admin, Manager |

### AI — `/api/ai`

| Method | Endpoint   | Description                    | Roles          |
|--------|------------|--------------------------------|----------------|
| POST   | `/suggest` | Generate subtasks from feature | Admin, Manager |

---

## 🔌 WebSocket Architecture

### Socket Auth Middleware (`middleware/socketAuth.js`)

```js
const jwt = require('jsonwebtoken');

const socketAuth = (socket, next) => {
  const token = socket.handshake.auth?.token;

  if (!token) {
    return next(new Error('Authentication error: no token provided'));
  }

  try {
    const decoded = jwt.verify(token, process.env.JWT_SECRET);
    socket.user = decoded; // attach user to socket instance
    next();
  } catch (err) {
    return next(new Error('Authentication error: invalid token'));
  }
};

module.exports = socketAuth;
```

### Socket Init (`sockets/index.js`)

```js
const socketAuth          = require('../middleware/socketAuth');
const taskHandler         = require('./handlers/task.handler');
const projectHandler      = require('./handlers/project.handler');
const notificationHandler = require('./handlers/notification.handler');
const presenceHandler     = require('./handlers/presence.handler');

const initSockets = (io) => {
  io.use(socketAuth); // apply JWT auth to ALL socket connections

  io.on('connection', (socket) => {
    console.log(`User connected: ${socket.user.name} [${socket.id}]`);

    // Join personal room for direct notifications
    socket.join(`user:${socket.user.id}`);

    // Register event handlers
    taskHandler(io, socket);
    projectHandler(io, socket);
    notificationHandler(io, socket);
    presenceHandler(io, socket);

    socket.on('disconnect', () => {
      console.log(`User disconnected: ${socket.user.name}`);
    });
  });
};

module.exports = initSockets;
```

### Event Name Constants (`sockets/events.js`)

```js
module.exports = {
  // Task events
  TASK_CREATED:         'task:created',
  TASK_UPDATED:         'task:updated',
  TASK_DELETED:         'task:deleted',
  TASK_STATUS_CHANGED:  'task:statusChanged',

  // Project events
  PROJECT_UPDATED:      'project:updated',
  MEMBER_ADDED:         'project:memberAdded',
  MEMBER_REMOVED:       'project:memberRemoved',

  // Notification events
  NOTIFICATION_NEW:     'notification:new',

  // Presence events
  USER_ONLINE:          'presence:online',
  USER_OFFLINE:         'presence:offline',
  PRESENCE_LIST:        'presence:list',

  // Room helper
  PROJECT_ROOM:         (projectId) => `project:${projectId}`,
};
```

### Task Handler (`sockets/handlers/task.handler.js`)

```js
const EVENTS = require('../events');

const taskHandler = (io, socket) => {

  // Join a project room
  socket.on('joinProject', ({ projectId }) => {
    socket.join(EVENTS.PROJECT_ROOM(projectId));
    console.log(`${socket.user.name} joined room: project:${projectId}`);
  });

  // Leave a project room
  socket.on('leaveProject', ({ projectId }) => {
    socket.leave(EVENTS.PROJECT_ROOM(projectId));
  });

  // Broadcast task status change
  socket.on(EVENTS.TASK_STATUS_CHANGED, ({ projectId, taskId, status }) => {
    io.to(EVENTS.PROJECT_ROOM(projectId)).emit(EVENTS.TASK_STATUS_CHANGED, {
      taskId,
      status,
      updatedBy:  socket.user.name,
      timestamp:  new Date().toISOString()
    });
  });

  // Broadcast new task
  socket.on(EVENTS.TASK_CREATED, ({ projectId, task }) => {
    io.to(EVENTS.PROJECT_ROOM(projectId)).emit(EVENTS.TASK_CREATED, { task });
  });

  // Broadcast task deletion
  socket.on(EVENTS.TASK_DELETED, ({ projectId, taskId }) => {
    io.to(EVENTS.PROJECT_ROOM(projectId)).emit(EVENTS.TASK_DELETED, { taskId });
  });
};

module.exports = taskHandler;
```

### Presence Handler (`sockets/handlers/presence.handler.js`)

```js
const EVENTS      = require('../events');
const onlineUsers = new Map(); // userId → { name, socketId }

const presenceHandler = (io, socket) => {

  socket.on('joinProject', ({ projectId }) => {
    onlineUsers.set(socket.user.id, {
      name:     socket.user.name,
      socketId: socket.id
    });

    // Tell others this user is online
    socket.to(EVENTS.PROJECT_ROOM(projectId)).emit(EVENTS.USER_ONLINE, {
      userId: socket.user.id,
      name:   socket.user.name
    });

    // Send current online list to joining user
    socket.emit(EVENTS.PRESENCE_LIST, Array.from(onlineUsers.entries()));
  });

  socket.on('disconnect', () => {
    onlineUsers.delete(socket.user.id);
    io.emit(EVENTS.USER_OFFLINE, { userId: socket.user.id });
  });
};

module.exports = presenceHandler;
```

### Notification Handler (`sockets/handlers/notification.handler.js`)

```js
const EVENTS = require('../events');

const notificationHandler = (io, socket) => {
  socket.on(EVENTS.NOTIFICATION_NEW, ({ toUserId, message, type }) => {
    io.to(`user:${toUserId}`).emit(EVENTS.NOTIFICATION_NEW, {
      message,
      type,       // 'task_assigned' | 'member_added' | 'task_updated'
      timestamp:  new Date().toISOString()
    });
  });
};

// Called from REST controllers to push server-side notifications
const sendNotification = (io, toUserId, payload) => {
  io.to(`user:${toUserId}`).emit(EVENTS.NOTIFICATION_NEW, {
    ...payload,
    timestamp: new Date().toISOString()
  });
};

module.exports = notificationHandler;
module.exports.sendNotification = sendNotification;
```

---

## 🔗 Emitting WebSocket Events from REST Controllers

When a REST route mutates data, it also broadcasts the change in real time.

```js
// controllers/task.controller.js
const taskService = require('../services/task.service');
const EVENTS      = require('../sockets/events');

const updateTask = async (req, res) => {
  const { id }                      = req.params;
  const { status, title, priority } = req.body;

  const updatedTask = await taskService.updateTask(id, { status, title, priority });

  // Broadcast to all users in the project room
  const io = req.app.get('io');
  io.to(EVENTS.PROJECT_ROOM(updatedTask.project_id)).emit(EVENTS.TASK_STATUS_CHANGED, {
    taskId:    updatedTask.id,
    status:    updatedTask.status,
    updatedBy: req.user.name,
    timestamp: new Date().toISOString()
  });

  res.json(updatedTask);
};

module.exports = { updateTask };
```

---

## 🌊 Full Request Flows

### REST Only
```
Client HTTP Request
    ↓
Routes           → defines method + path
    ↓
verifyToken      → confirms valid JWT
    ↓
checkRole        → confirms user has required role
    ↓
Controllers      → handles req/res
    ↓
Services         → business logic
    ↓
Models           → DB queries
    ↓
PostgreSQL
```

### WebSocket Only
```
Client emits event  e.g. task:statusChanged
          ↓
socketAuth middleware  → JWT verified on connection
          ↓
sockets/handlers/task.handler.js  → receives event
          ↓
io.to('project:1').emit(...)  → broadcasts to room
          ↓
All project members receive update instantly
```

### REST + WebSocket Combined
```
Client calls PUT /api/tasks/:id
          ↓
verifyToken + checkRole pass
          ↓
task.controller.js updates DB via service → model
          ↓
Controller calls io.to(room).emit(event)
          ↓
REST response  → 200 OK           (back to caller)
Socket event   → task:statusChanged  (to all room members)
```

---

## 🗺️ WebSocket Events Summary

| Event Name             | Direction       | Triggered When                    |
|------------------------|-----------------|-----------------------------------|
| `task:statusChanged`   | Client ↔ Server | Task dragged to new Kanban column |
| `task:created`         | Server → Room   | New task added to project         |
| `task:deleted`         | Server → Room   | Task removed from project         |
| `project:memberAdded`  | Server → Room   | Manager adds a new member         |
| `notification:new`     | Server → User   | Task assigned to you              |
| `presence:online`      | Server → Room   | Teammate opens the project        |
| `presence:offline`     | Server → Room   | Teammate closes or disconnects    |
| `presence:list`        | Server → User   | You open a project                |

---

## ⚙️ Environment Variables (`.env`)

```env
PORT=5000
CLIENT_URL=http://localhost:5173
DATABASE_URL=postgresql://user:password@localhost:5432/devsync
JWT_SECRET=your_access_token_secret
JWT_REFRESH_SECRET=your_refresh_token_secret
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d
GEMINI_API_KEY=your_gemini_api_key
NODE_ENV=development
```

---

## 📦 All Dependencies

```json
{
  "dependencies": {
    "express":               "^4.18.2",
    "pg":                    "^8.11.0",
    "bcryptjs":              "^2.4.3",
    "jsonwebtoken":          "^9.0.0",
    "socket.io":             "^4.6.1",
    "dotenv":                "^16.0.3",
    "cors":                  "^2.8.5",
    "express-validator":     "^7.0.1",
    "@google/generative-ai": "^0.1.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
```

Frontend will also need:
```json
{
  "socket.io-client": "^4.6.1"
}
```

---

## 🧱 3-Layer Architecture

```
┌──────────────────────────────────────────────┐
│             Presentation Layer               │
│   routes/ · verifyToken · checkRole          │
│   sockets/index.js · socketAuth              │
├──────────────────────────────────────────────┤
│             Application Layer                │
│   controllers/ · services/                   │
│   sockets/handlers/ · sockets/events.js      │
├──────────────────────────────────────────────┤
│               Data Layer                     │
│         models/ · PostgreSQL                 │
└──────────────────────────────────────────────┘
```

> Controllers never query the DB directly.  
> Models never contain business logic.  
> Sockets never bypass the auth middleware.  
> Every layer has one job.
